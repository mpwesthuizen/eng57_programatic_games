# How to remove list duplicates using sets


#
# no_dup = list(set(test))
# print(no_dup)

def no_dup(old_list):
    new_list = []
    for arg in old_list:
        new_list.append(arg)
    return list(set(new_list))

